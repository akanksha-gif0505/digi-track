# VITE_API_BASE_URL: Backend API base URL (used when running frontend standalone)
# In development, the Vite proxy rewrites /api/* to the backend automatically.
# Only set this if deploying the frontend to a separate domain/CDN.
VITE_API_BASE_URL="http://localhost:3000"
